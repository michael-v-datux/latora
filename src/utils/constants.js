/**
 * constants.js — Константи та тема додатка
 * 
 * Тут зібрані всі кольори, розміри та налаштування,
 * щоб змінювати їх в одному місці замість пошуку по всьому коду.
 */

// Кольори CEFR рівнів (від найлегшого A1 до найскладнішого C2)
export const CEFR_COLORS = {
  A1: '#16a34a',  // зелений — найлегші слова
  A2: '#65a30d',  // салатовий
  B1: '#ca8a04',  // жовтий — середній рівень
  B2: '#ea580c',  // оранжевий
  C1: '#dc2626',  // червоний — складні
  C2: '#991b1b',  // темно-червоний — найскладніші
};

// Опис кожного CEFR рівня (для відображення в інтерфейсі)
export const CEFR_LABELS = {
  A1: 'Початковий',
  A2: 'Елементарний',
  B1: 'Середній',
  B2: 'Вище середнього',
  C1: 'Просунутий',
  C2: 'Досконалий',
};

// Основна палітра кольорів додатка (світла тема, мінімалізм)
export const COLORS = {
  // Основні
  primary: '#0f172a',     // майже чорний — основний текст
  accent: '#2563eb',      // синій — акцентний колір
  background: '#f6f7f9',  // світло-сірий — фон додатка
  surface: '#ffffff',     // білий — фон карток
  
  // Текст
  textPrimary: '#0f172a',   // основний текст
  textSecondary: '#64748b', // допоміжний текст
  textMuted: '#b6b6b6',     // приглушений текст
  textHint: '#b6b6b6',      // підказки, плейсхолдери
  
  // Межі та розділювачі
  border: '#e2e8f0',        // лінії між елементами
  borderLight: '#f1f5f9',   // ледь помітні лінії
  
  // Статуси
  success: '#16a34a',       // зелений — успіх
  warning: '#ea580c',       // оранжевий — попередження
  error: '#dc2626',         // червоний — помилка
  info: '#2563eb',          // синій — інформація
};

// Розміри шрифтів
export const FONT_SIZES = {
  xs: 10,
  sm: 12,
  md: 14,
  lg: 16,
  xl: 18,
  xxl: 24,
  title: 28,
};

// Відступи (використовуємо кратні 4 для консистентності)
export const SPACING = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
};

// Радіуси заокруглення
export const BORDER_RADIUS = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  full: 9999,
};

// URL бекенду (змінити на продакшн-адресу пізніше)
// Для розробки на комп'ютері використовуємо localhost
export const API_BASE_URL = __DEV__ 
  // ? 'http://localhost:3001/api'  // локальний сервер при розробці
  ? 'http://192.168.0.150:3001/api'  // локальний сервер при розробці
  : 'https://your-production-server.com/api'; // продакшн (замінити пізніше)
